<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />

  <!-- AOS (Animate on Scroll) -->
  <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
  <title>SG App</title>
  <style>
    .custom-navbar {
      background-color: #ff0000;
      /* Bright red */
    }

    .custom-navbar .nav-link {
      color: white !important;
    }

    .custom-navbar .navbar-brand {
      color: white !important;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: 'Poppins', sans-serif;
      background: #f0f4f8;
      color: #333;
      line-height: 1.6;
    }

    header {
      background: linear-gradient(135deg, #007bff, #00c6ff);
      padding: 25px 20px;
      color: white;
      text-align: center;
      position: sticky;
      top: 0;
      z-index: 1000;
    }

    nav {
      margin-top: 10px;
    }

    nav a {
      margin: 0 15px;
      color: #ffffffdd;
      text-decoration: none;
      font-weight: 600;
      transition: color 0.3s ease;
    }

    nav a:hover {
      color: #fff;
    }

    .hero {
      text-align: center;
      padding: 80px 20px 40px;
      position: relative;
      background: url('https://www.transparenttextures.com/patterns/white-wall-3.png');
    }

    .hero h1 {
      font-size: 2.5rem;
      font-weight: 700;
      color: #007acc;
    }

    .hero p {
      font-size: 1.1rem;
      color: white;
      max-width: 600px;
      margin: 20px auto;
    }

    .search-container {
      margin-top: 30px;
      display: flex;
      justify-content: center;
      gap: 10px;
      flex-wrap: wrap;
    }

    .search-container input[type="search"] {
      padding: 12px 18px;
      border: 2px solid #007acc;
      border-radius: 30px;
      font-size: 1rem;
      width: 280px;
    }

    .search-container button {
      padding: 12px 25px;
      border: none;
      border-radius: 30px;
      background: linear-gradient(to right, #1e90ff, #00c6ff);
      color: white;
      font-weight: 600;
      cursor: pointer;
    }

    .buttons {
      margin-top: 30px;
    }

    .btn {
      margin: 10px 15px;
      padding: 14px 30px;
      border-radius: 30px;
      background: linear-gradient(to right, #cc0000, #ff2200);
      color: white;
      font-size: 1.1rem;
      font-weight: 600;
      box-shadow: 0 5px 10px rgba(0, 122, 204, 0.3);
      text-decoration: none;
      transition: transform 0.3s ease;
      display: inline-block;
    }

    .btn:hover {
      transform: translateY(-3px);
    }

    .stats {
      text-align: center;
      margin-top: 60px;
    }

    .stats h3 {
      font-size: 1.4rem;
      color: #222;
    }

    .cards {
      display: flex;
      justify-content: center;
      gap: 20px;
      flex-wrap: wrap;
      margin: 60px 20px;
    }

    .card {
      background: white;
      border-radius: 16px;
      padding: 20px;
      width: 280px;
      text-align: center;
      box-shadow: 0 8px 15px rgba(0, 0, 0, 0.1);
      transition: transform 0.3s ease;
    }

    .card:hover {
      transform: translateY(-5px);
    }

    .card img {
      width: 60px;
      margin-bottom: 15px;
    }

    footer {
      background: #e7f0ff;
      text-align: center;
      padding: 20px;
      font-size: 0.9rem;
      color: #555;
    }

    footer a {
      margin: 0 10px;
      color: #007acc;
      text-decoration: none;
    }

    footer a:hover {
      color: #005c99;
    }

    @media (max-width: 600px) {
      .cards {
        flex-direction: column;
        align-items: center;
      }
    }
  </style>
</head>

<body>
  <nav class="navbar navbar-expand-sm custom-navbar">
    <div class="container-fluid">
      <% if (user && user.role==='admin' ) { %>
        <a class="navbar-brand" href="/admin_bal">SG App</a>
      <% } else { %>
        <a class="navbar-brand" href="/dashboard">SG App</a>
      <% } %>
            <img src="/images/Logo.png" alt="Logo" style="width:40px;" class="rounded-pill">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
              <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="collapsibleNavbar">
              <ul class="navbar-nav">
                <li class="nav-item">
                  <a class="nav-link" href="/addopportunities">Add Opportunities</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link" href="/logout">Logout</a>
                </li>
              </ul>
            </div>
    </div>
  </nav>
  <!-- Hero 5 - Bootstrap Brain Component -->
  <div class="jumbotron text-center">
    <div class="text-center mt-4">
      <div class="ratio ratio-16x9" style="width: 100%; max-width: 1200px; margin: auto;">
        <iframe src="https://www.youtube.com/embed/9Laga0bgRq4" title="YouTube video" allowfullscreen></iframe>
      </div>
    </div>
  </div>
  </div>
  </div>


  <h2 style="text-align: center;">SG volunteer opportunities</h2><br><br>

  <form action="/sg__volunteer_opportunities/search" method="POST" class="d-flex mb-4" role="search">
    <input class="form-control me-2" type="text" name="keyword" placeholder="Search by name or organization" required>
    <button class="btn btn-outline-primary" type="submit">Search</button>
  </form><br><br>

  <table class="table table-hover small text-center">
    <thead>
      <tr>
        <!-- <th width="100">Opportunities Id</th> -->
        <th width="100">Opportunities Name</th>
        <th width="100">Opportunities Image</th>
        <th width="50">Organization</th>
        <th width="50">Date</th>
        <th width="50">Location</th>
        <th width="50">Contact Email</th>
        <th width="50">Edit</th>
        <th width="50">Delete</th>
      </tr>
    </thead>
    <tbody>
      <% for(let i=0; i < opportunities.length; i++) { %>
        <tr><!--<a href="/opportunities/<= opportunities[i].opportunitiesId %>" ></a>-->
          <!-- <td><%= opportunities[i].opportunitiesId %></td> -->
          <td>
            <%= opportunities[i].opportunitiesName %>
          </td>
          <td><img src="images/<%= opportunities[i].image %>" width="50%"></td>
          <td>
            <%= opportunities[i].organization %>
          </td>
          <td>
            <%= opportunities[i].date %>
          </td>
          <td>
            <%= opportunities[i].location %>
          </td>
          <td>
            <%= opportunities[i].contact_email %>
          </td>
          <!-- Edit Link -->
          <td><a href="/updateopportunities/<%= opportunities[i].opportunitiesId %>">Edit</a></td>
          <!-- Delete Link -->
          <td><a href="/deleteopportunities/<%= opportunities[i].opportunitiesId %>"
              onclick="return confirm('Are you sure you want to delete this opportunities?')">Delete</a></td>
        </tr>
        <% } %>
    </tbody>
  </table>
  </div>
  <footer>
    <p>&copy; 2025 SG Community & Leaderboard. All rights reserved.</p>
    <div>
      <a href="#"><i class="fab fa-facebook"></i></a>
      <a href="#"><i class="fab fa-twitter"></i></a>
      <a href="#"><i class="fab fa-linkedin"></i></a>
    </div>
  </footer>

  <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
  <script>
    AOS.init();
  </script>
</body>

</html>