<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>SG Community & Leaderboard</title>

  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
  <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">

  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body {
      font-family: 'Poppins', sans-serif;
      background: #f0f4f8;
      color: #333;
      line-height: 1.6;
    }
    header {
      background: linear-gradient(135deg, #007bff, #00c6ff);
      padding: 25px 20px;
      color: white;
      text-align: center;
      position: sticky;
      top: 0;
      z-index: 1000;
    }
    nav { margin-top: 10px; }
    nav a {
      margin: 0 15px;
      color: #ffffffdd;
      text-decoration: none;
      font-weight: 600;
      transition: color 0.3s ease;
    }
    nav a:hover { color: #fff; }

    .hero {
      text-align: center;
      padding: 80px 20px 40px;
      background: url('https://www.transparenttextures.com/patterns/white-wall-3.png');
    }
    .hero h1 {
      font-size: 2.5rem;
      font-weight: 700;
      color: #007acc;
    }
    .hero p {
      font-size: 1.1rem;
      color: #555;
      max-width: 600px;
      margin: 20px auto;
    }
    .search-container {
      margin-top: 30px;
      display: flex;
      justify-content: center;
      gap: 10px;
      flex-wrap: wrap;
    }
    .search-container input[type="search"] {
      padding: 12px 18px;
      border: 2px solid #007acc;
      border-radius: 30px;
      font-size: 1rem;
      width: 280px;
    }
    .search-container button {
      padding: 12px 25px;
      border: none;
      border-radius: 30px;
      background: linear-gradient(to right, #1e90ff, #00c6ff);
      color: white;
      font-weight: 600;
      cursor: pointer;
    }
    .buttons { margin-top: 30px; }
    .btn {
      margin: 10px 15px;
      padding: 14px 30px;
      border-radius: 30px;
      background: linear-gradient(to right, #007acc, #00c6ff);
      color: white;
      font-size: 1.1rem;
      font-weight: 600;
      box-shadow: 0 5px 10px rgba(0, 122, 204, 0.3);
      text-decoration: none;
      transition: transform 0.3s ease;
      display: inline-block;
    }
    .btn:hover { transform: translateY(-3px); }

    .feedback-section {
      margin: 50px auto;
      text-align: center;
    }
    .feedback-card {
      background: white;
      border-left: 6px solid #00bcd4;
      box-shadow: 0px 4px 12px rgba(0,0,0,0.1);
      margin: 15px auto;
      padding: 20px;
      border-radius: 10px;
      max-width: 500px;
    }
    .feedback-card h5 { margin-bottom: 5px; }
    .feedback-card p { margin-bottom: 0; }
    .feedback-card .stars { color: gold; }
  </style>
</head>
<body>
  <header>
    <h1>Welcome to SG Community & Leaderboard</h1>
    <nav>
      <a href="/login">Login</a>
      <a href="/register">Register</a>
      <a href="/leaderboard">Leaderboard</a>
      <a href="/events">Events</a>
      <a href="/feedback">Feedback</a>
    </nav>
  </header>

<section class="hero" data-aos="fade-up">
    <h1>Engage. Contribute. Be Recognized.</h1>
    <p>This platform encourages community engagement and showcases top contributors through a dynamic leaderboard system.</p>
    <div class="search-container">
      <input type="search" placeholder="Search the community or leader">
      <button type="submit">Search</button>
    </div>
    <div class="buttons">
      <a href="/login" class="btn">Get Started</a>
      <a href="/leaderboard" class="btn">View Leaderboard</a>
      <a href="/events" class="btn">View Events</a>
      <a href="/feedback" class="btn">View Feedback</a>
    </div>
</section>

<!-- Feedback teaser -->
<section class="feedback-section" data-aos="fade-up">
  <h3>Community Feedback</h3>
  <p>See what people are saying about our events.</p>

  <% if (feedbacks.length > 0) { %>
    <% feedbacks.forEach(fb => { %>
      <div class="feedback-card">
        <h5><%= fb.events_name %></h5>
        <p><em>- <%= fb.attendee_name %></em></p>
        <p class="stars">
          <% for (let i = 0; i < fb.rating; i++) { %> ★ <% } %>
        </p>
        <p>"<%= fb.comments %>"</p>
      </div>
    <% }) %>
  <% } else { %>
    <p>No feedback available yet.</p>
  <% } %>
</section>

<footer>
  <p>&copy; 2025 SG Community & Leaderboard. All rights reserved.</p>
  <div>
    <a href="#"><i class="fab fa-facebook"></i></a>
    <a href="#"><i class="fab fa-twitter"></i></a>
    <a href="#"><i class="fab fa-linkedin"></i></a>
  </div>
</footer>

<script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
<script>AOS.init();</script>
</body>
</html>

