<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>SG Community & Leaderboard</title>

    <!-- Google Font -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />

    <!-- AOS (Animate on Scroll) -->
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">

    <!-- Bootstrap -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">

    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: #f0f4f8;
            color: #333;
            line-height: 1.6;
            height: 100vh; /* full height for flexbox centering */
        }

        header {
            background: linear-gradient(135deg, #007bff, #00c6ff);
            padding: 25px 20px;
            color: white;
            text-align: center;
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        nav {
            margin-top: 10px;
        }

        nav a {
            margin: 0 15px;
            color: #ffffffdd;
            text-decoration: none;
            font-weight: 600;
            transition: color 0.3s ease;
        }

        nav a:hover {
            color: #fff;
        }

        /* === Centered Content === */
        .center-content {
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            height: calc(100vh - 100px); /* subtract header height */
            text-align: center;
        }

        .btn-group-custom {
            margin-top: 20px;
        }

        /* Optional: Adjust spacing */
        h1, p {
            margin-bottom: 20px;
        }
    </style>
</head>

<body>
    <header>
        <h1>Welcome to SG Community & Leaderboard</h1>
    </header>

    <!-- Main centered content -->
    <div class="container center-content">
        <h1>Your login was successful</h1>

        <% if (user) { %>
            <p>Hello, <%= user.username %> (<%= user.role %>)</p>
            <p id="redirect-msg" class="mt-3 text-muted">Redirecting in <span id="countdown">3</span> seconds...</p>
            <script>
                let seconds = 3;
                const countdownEl = document.getElementById('countdown');

                const interval = setInterval(() => {
                    seconds--;
                    countdownEl.textContent = seconds;

                    if (seconds <= 0) {
                        clearInterval(interval);
                        window.location.href = '/'; // Change this to your homepage route
                    }
                }, 1000);
            </script>

            <div class="btn-group-custom">
                <a href="/logout" class="btn btn-danger mx-2">Logout</a>
            </div>
        <% } %>
    </div>
</body>
</html>
